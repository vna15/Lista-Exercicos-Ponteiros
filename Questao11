#include <iostream>



using namespace std;





int main(){



    double vet[4];

    int i;

    for (i=0; i<3; i++){

        printf("%d ",vet+i);

        }

  }



//Para as variáveis do tipo CHAR, a arquitetura do PC está alocando 1 bit para cada valor do vetor.

//Para as variáveis do tipo INT, a arquitetura do PC está alocando 4 bits para cada valor do vetor.

//Para as variáveis do tipo FLOAT, a arquitetura do PC está alocando 4 bits para cada valor do vetor.

//Para as variáveis do tipo DOUBLE, a arquitetura do PC está alocando 8 bits para cada valor do vetor.
